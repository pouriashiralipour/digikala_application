import 'package:flutter/material.dart';

class MagnetPage extends StatelessWidget {
  const MagnetPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
